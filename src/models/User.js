class User {
    constructor(username, password) {
      this.username = username;
      this.password = password; // In a real-world application, never store passwords in plain text
    }
  
    // You can add methods here to handle user-related logic
  }
  
  export default User;